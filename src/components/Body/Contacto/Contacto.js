import HeaderComponent from "../../Header/HeaderComponent";

const Contacto = () => {
    return(
        <section>
            <HeaderComponent />
        </section>
    )
}

export default Contacto;